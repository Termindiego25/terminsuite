# ================================================
# TerminSuite - OpenLDAP Environment Variables
# ================================================
# Base DN for the LDAP directory
LDAP_ROOT=dc=domain,dc=com

# Administrator credentials
LDAP_ADMIN_USERNAME_FILE: /run/secrets/admin_username
LDAP_ADMIN_PASSWORD_FILE: /run/secrets/admin_password

# Skip default LDAP tree and use custom LDIFs
LDAP_SKIP_DEFAULT_TREE=yes

# Directory containing custom LDIF files (read-only)
LDAP_CUSTOM_LDIF_DIR=/ldifs